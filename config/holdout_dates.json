{
  "enabled": false,
  "policy": "last_n_per_instrument",
  "n": 1,
  "notes": "Per-instrument reservation: for each instrument, the most recent N dates with raw or parquet data are reserved for backtest. enabled=false (set 2026-05-20 per PROJECT_TODO T24) turns the gate OFF — every date becomes available for replay/training. Re-enable when the paper-trade window starts. n / policy stay configured so flipping enabled=true picks up cleanly."
}
