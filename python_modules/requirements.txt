# Phase E1 — pinned 2026-04-30 against the live dev box (Python 3.14.3,
# Windows 11 x64). Anyone reproducing this stack must use these exact
# versions; bumping requires re-running MTA training and validating
# against `metrics.json` deltas.
#
# Networking / config
requests==2.32.5
python-dotenv==1.2.2
websockets==16.0
#
# Imaging (used by startup/make_icons.py)
Pillow==12.2.0
#
# ML pipeline (MTA + SEA)
lightgbm==4.6.0
pandas==3.0.1
numpy==2.4.3
pyarrow==23.0.1
scikit-learn==1.8.0
#
# Replay parallelism (T47 — Phase A)
#   rich         — multi-worker progress dashboard (rich.live.Live + Table)
#   portalocker  — cross-process file lock for replay_checkpoint.json under
#                  ProcessPoolExecutor fan-out (workers finish out of order)
rich==13.9.4
portalocker==2.10.1
