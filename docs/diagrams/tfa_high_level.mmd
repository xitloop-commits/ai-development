graph LR
    subgraph EXT["External Inputs"]
        WS["Broker WebSocket"]
        REST["Broker REST API"]
        IP["Instrument Profile"]
    end

    TFA["TickFeatureAgent\n(TFA)"]

    subgraph OUT["Output"]
        ML["ML Consumer"]
    end

    WS -->|"Underlying tick — sub-second"| TFA
    WS -->|"Option tick × ATM ±3 strikes"| TFA
    REST -->|"Chain snapshot — every 5s"| TFA
    IP -->|"exchange · symbol · securityId · session hours\n(once at startup)"| TFA
    TFA -->|"180-col flat vector — per tick"| ML
