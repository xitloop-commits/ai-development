// Terminal Noir Trading Dashboard — Type Definitions
// Re-export from shared types for consistency between server and client

export type {
  ModuleStatus,
  SupportResistance,
  ActiveStrike,
  Signal,
  SignalType,
  InstrumentData,
  Position,
  TradingMode,
  SRLevel,
  NewsDetail,
  NewsArticle,
  MarketHoliday,
  MarketEvent,
  SRIntradayLevel,
} from '../../../shared/tradingTypes';
